package com.example.inscripciones.models.enums;

public enum EstadoInscripcion {
    CURSANDO,   // El alumno está estudiando
    FINALIZADO, // Llegó al 100%
    ABANDONADO  // Se dio de baja (opcional)
}